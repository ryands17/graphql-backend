### A Sample GraphQL server using `graphql-yoga` and `knex`

#### Use yarn (recommended) or npm

- `yarn` or `npm i`
- `yarn dev` or `npm run dev`
- Open gql playground on http://localhost:4000
- Explore and edit as you wish!
