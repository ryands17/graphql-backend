type User {
  name: String!
  email: String!
}

type LoginSuccess {
  token: String!
}

input UserAuthInput {
  email: String!
  password: String!
}

type Mutation {
  signup(name: String!, email: String!, password: String!): Boolean!

  login(credentials: UserAuthInput!): LoginSuccess!
}
